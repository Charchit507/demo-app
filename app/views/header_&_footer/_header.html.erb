<nav class="navbar navbar-expand-lg navbar-dark bg-success">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Blog Application</a>
    <div class="row justify-content-end">
      <div class="nav-link">
        <% if user_signed_in? %>
          <%= link_to 'Sign out', destroy_user_session_path, method: :delete, style: "color:white;text-decoration:none;" %>
        <% else %>
          <%= link_to 'Sign in', new_user_session_path, style: "color:white;text-decoration:none;" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
